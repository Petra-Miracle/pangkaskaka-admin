import { PlaceholderPage } from "@/components/nav/placeholder-page";

export default function ShopsPage() {
  return (
    <PlaceholderPage
      title="Shops"
      blocked
      description="Full shop directory and suspend action. Needs GET /admin/shops?status=&search=&page= — not yet implemented in the backend. See AGENT_BRIEF.md section 4."
    />
  );
}
