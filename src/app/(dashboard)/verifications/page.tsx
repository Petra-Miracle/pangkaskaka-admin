import { PlaceholderPage } from "@/components/nav/placeholder-page";

export default function VerificationsPage() {
  return (
    <PlaceholderPage
      title="Verifications"
      description="Shop verification queue from GET /admin/pending-shops, with per-document review and the verification chat thread. Ships in Phase 1."
    />
  );
}
