import { PlaceholderPage } from "@/components/nav/placeholder-page";

export default function BookingsPage() {
  return (
    <PlaceholderPage
      title="Bookings"
      blocked
      description="Cross-shop booking search for dispute support. Needs GET /admin/bookings?shop_id=&status=&search=&page= — not yet implemented. See AGENT_BRIEF.md section 4."
    />
  );
}
